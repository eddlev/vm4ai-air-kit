# AIR HR Prompt Edition

Human-readable, familiar AIR prompts for existing users, manual attachment, audit, learning, and compatibility.

The complete prompt files are byte-preserved from the current root prompt set. HR means human-readable, not weaker or obsolete governance.

Root prompt paths remain compatibility aliases during migration.

Commands: `air edition hr`, `air edition hr once`, `air edition status`, `air edition compare`.
