name: AIR Handoff v0.7.4 full-tree validation runner
on:
  push:
    branches: [air/handoff-v074-validation-20260917]
permissions:
  contents: write
jobs:
  validate:
    if: ${{ contains(github.event.head_commit.message, '[air-handoff-v074-runner]') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Verify exact f456 base and temporary-only prepatch delta
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor f456fc579bed739aebcfaa55c175c2c14617d58d HEAD
          git diff --name-only f456fc579bed739aebcfaa55c175c2c14617d58d..HEAD | sort > /tmp/prepatch.txt
          printf '%s\n' '.github/workflows/handoff-v074-validation-runner.yml' '.github/workflows/handoff-v074-validation.yml' | sort > /tmp/expected.txt
          diff -u /tmp/expected.txt /tmp/prepatch.txt
      - name: Recover approved payload from temporary carrier workflow
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .air-validation
          awk '/^[[:space:]]*\/Td6WFo/{capture=1} capture{sub(/^[[:space:]]*/,""); print} /^[[:space:]]*PAYLOAD$/{exit}' .github/workflows/handoff-v074-validation.yml | sed '$d' > /tmp/payload.b64
          test -s /tmp/payload.b64
          base64 -d /tmp/payload.b64 | xz -d | tar -xf - -C .air-validation
          rm /tmp/payload.b64
          test -f .air-validation/helpers/apply_air_v074_handoff_release_candidate.py
      - name: Apply confirmed patch and coordinated reseal
        run: python3 .air-validation/helpers/apply_air_v074_handoff_release_candidate.py . .air-validation .air-validation/helpers/apply_handoff_runtime_patch.py
      - name: Remove temporary validation harness
        run: rm -rf .air-validation .github/workflows/handoff-v074-validation.yml .github/workflows/handoff-v074-validation-runner.yml
      - name: Enforce no leaked temporary scaffolding
        shell: bash
        run: |
          set -euo pipefail
          test -z "$(git status --porcelain | grep -E '(\.air-validation|handoff-v074-validation)' || true)"
      - name: Canonical AIR candidate validation suite
        run: python3 tools/validate_air_suite.py --candidate
      - name: Canonical AIR exact-release validation suite
        run: python3 tools/validate_air_suite.py
      - name: Verify coordinated reseal fixed point
        run: python3 tools/reseal_air_candidate.py --check
      - name: Commit validated canonical patch to temporary branch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Validate AIR v0.7.4 Handoff runtime patch [air-handoff-v074-validated]'
          test -z "$(git diff --name-only f456fc579bed739aebcfaa55c175c2c14617d58d..HEAD | grep -E '^(\.air-validation/|\.github/workflows/handoff-v074-validation(-runner)?\.yml$)' || true)"
          git push origin HEAD:air/handoff-v074-validation-20260917
