name: AIR v0.7.3 transport probe

on:
  push:
    branches:
      - "release/v0.7.3-patch2"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "AIR v0.7.3 transport probe PASS"
