name: AIR v0.7.1 set-005 one-run repair

on:
  push:
    branches:
      - repair/v0.7.1-set005-hardening

permissions:
  contents: write

jobs:
  apply-repair:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner disabled while payload transport is repaired"
